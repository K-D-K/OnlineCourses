package entity

import "OnlineCourses/interfaces"

// Need to add generic handling like clone, after clone, validations by iterating each records

func updateParentIds(entityGroup *interfaces.EntityGroup) {

}
